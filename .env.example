# Copy to .env.local and put a real key in it.
# Get one from https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Postgres. Supabase, Neon, anything. Omit it and the app still runs,
# but a plan will not survive the tab closing.
DATABASE_URL=postgres://user:pass@host:5432/dbname
